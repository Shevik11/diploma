[pytest]
# Local pytest config for scripts/tests/test_e2e_results.py
# Registers custom markers and silences PytestUnknownMarkWarning.
markers =
    slow: full pipeline test that runs run_all_tests.py end-to-end (opt in with --run-slow)
